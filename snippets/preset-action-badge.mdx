export const PresetActionBadgeBrowser = () => {
  return (
    <div
      style={{
        borderRadius: "0.375rem",
        fontSize: "0.75rem",
        lineHeight: "1.5rem",
        fontWeight: 400,
        backgroundColor: '#ffedd5',
        color: '#9a3412',
        border: 'solid 1px #fdba74',
        textAlign: 'center',
        padding: '0 6px'
      }}
    >
      Browser
    </div>
  );
};

export const PresetActionBadgeInteractive = () => {

  return (
    <div
      style={{
        borderRadius: "0.375rem",
        fontSize: "0.75rem",
        lineHeight: "1.5rem",
        fontWeight: 400,
        backgroundColor: '#dbeafe',
        color: '#1e40af',
        border: 'solid 1px #93c5fd',
        textAlign: 'center',
        padding: '0 6px'
      }}
    >
      Interactive
    </div>
  );
};